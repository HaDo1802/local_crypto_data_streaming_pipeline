# Copy to .env and adjust for local runs (docker compose reads these automatically).

POSTGRES_DB=crypto
POSTGRES_USER=trader
POSTGRES_PASSWORD=trader

MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
